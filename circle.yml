machine:
  java:
    version: openjdk7
general:
  artifacts: 
    - build/libs
dependencies:
  cache_directories:
    - ".gradle"
    - "build"
  pre:
    - chmod +x gradlew
test:
  pre:
    - ./gradlew build
