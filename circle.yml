machine:
  java:
    version: openjdk7
general:
  artifacts: 
    - build/libs
dependencies:
  pre:
    - chmod +x gradlew
  post:
    - ./gradlew setupCIWorkspace
  cache_directories:
    - ".gradle"
    - "build"
test:
  pre:
    - ./gradlew build
  override:
    - ./gradlew test
